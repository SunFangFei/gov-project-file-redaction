---
name: gov-project-file-redaction
description: 对政府信息化建设方案、可研报告、技术建议书、投资估算表及其附件进行本地脱敏，支持 DOCX、XLSX 和 PDF，保留版式、层级、公式及可审阅性，并输出脱敏映射表和风险报告。用户要求文件脱敏、匿名化、对外送审版、厂商隐去版或上传 AI 前处理时使用。
---

# 方案文件脱敏

生成可安全流转且仍可评审、校对和对账的副本。默认只在本地处理，不覆盖源文件，不将原始敏感值提交给外部模型或在线服务。

## 开始前

先确认或明确写出以下范围；缺失但可合理推断时，采用默认值并在报告中说明：

- 使用场景：上传 AI、跨部门评审、对外询价、公开发布或内部共享。
- 脱敏强度：轻度、标准、严格。默认“标准”。
- 是否保留项目名称、行政区划、建设单位、金额和供应商之间的对应关系。
- 金额策略：保留、统一比例缩放、区间化、完全移除。投资评审场景默认保留金额；上传外部 AI 场景默认先询问。
- 输出目录和命名。默认生成新文件 `原文件名_脱敏版.ext`，不得覆盖源文件。
- 多文件是否采用同一映射。默认同一批文件使用同一映射，确保方案与投资表可核对。

不得仅凭文件名判断内容。先检查正文、表格、页眉页脚、批注、修订、隐藏内容、文档属性及嵌入对象。

## 工作流

1. 建立文件清单，记录源文件哈希、格式、页数或工作表数以及是否加密。
2. 读取 [references/redaction-policy.md](references/redaction-policy.md)，按使用场景确定字段等级、金额策略和替换方式。
3. 扫描并形成候选敏感项清单，列明位置、原值类型、建议替换值、置信度及处理理由。不要在面向外部的日志中复述完整原值。
4. 对高影响或语义不明确的候选项先请用户确认；手机号、身份证号、账号、密码、密钥、私有 IP 等高风险字段可直接纳入脱敏范围。
5. 建立批次级映射表。同一实体必须稳定替换；不同实体不得合并成同一占位符。金额不得逐格随机修改。
6. 按文件类型执行最小必要修改：
   - DOCX：读取 [references/docx.md](references/docx.md)。
   - XLSX：读取 [references/xlsx.md](references/xlsx.md)。
   - PDF：读取 [references/pdf.md](references/pdf.md)。
7. 用 `scripts/verify_redaction.py` 对输出文件和禁止残留词表做反向检索；再进行格式级、金额一致性和视觉核验。
8. 输出脱敏文件、内部映射表和风险报告。报告结构见 [references/reporting.md](references/reporting.md)。

## 核心判断

- 先区分必须删除的身份信息和评审所需的业务信息。建设内容、性能指标、投资结构、政策名称通常保留。
- 项目名称、单位名称、厂商名称等根据场景处理：内部评审通常保留；上传外部 AI 或跨项目复用时默认替换。
- 金额不一律清零。对投资评审文件优先保持数量×单价=合价、分项合计=总计及方案与预算的一致性。
- 若采用比例缩放，同一批次所有正文金额、单价、合价、小计、总计和核增减额使用同一系数；数量通常不缩放。
- 项目、建设单位、供应商等采用稳定占位符，如“项目A”“建设单位A”“供应商A”；人员用“联系人A”；系统、医院、街镇等按实体类型编号。
- 不改政策文件名称、标准编号和公开机构名称，除非其组合足以定位未公开项目。
- 不把删除批注误认为已处理修订内容；不把隐藏工作表视为已删除；不把 PDF 黑色矩形遮盖视为真正脱敏。
- 无法可靠保持版式、公式或对象级删除时停止交付，并说明能力缺口，不声称已安全脱敏。

## 输出要求

至少交付：

1. 脱敏文件，源文件保持不变。
2. `脱敏映射表.xlsx` 或等价受控文件，仅供授权人员保存，不与对外文件一起发送。
3. `脱敏风险报告.md`，记录处理范围、金额策略、规则、验证、残余风险和人工复核项。

映射表属于高敏感文件。除非用户明确要求，不在聊天正文展示原值与替换值的完整对应关系。

## 验收底线

- 方案与投资估算表中的占位符前后一致。
- 标题层级、目录、表格、编号、公式、合并单元格、分页及主要版式未被破坏。
- 金额处理后，正文、总表、分表、数量、单价、合价、核增减额与总投资之间仍一致；无法重算时必须标为待人工复核。
- DOCX 的修订、批注、页眉页脚和属性已检查；XLSX 的隐藏表、批注、名称、链接及属性已检查；PDF 的文本层、图片层、批注、附件、元数据和历史对象已检查。
- 禁止残留词表检索为零，或每一处例外均有书面说明。
- 抽查不得替代高风险字段和金额公式的全量复核。
